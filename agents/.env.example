# Helius developer plan API key — Observer subscribes to LaserStream.
HELIUS_API_KEY=

# Comma-separated Solana account addresses to monitor (LP positions, pool
# accounts, lending accounts). Builder A — populate from policy on D3.
POSITION_IDS=
