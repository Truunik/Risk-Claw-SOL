[workspace]
# Reference host scaffolded by `arcium init` was deleted; the real host
# program lives in /programs/programs/risk_policy and gets wired to this
# circuit's comp_def in PRD task C-14.
members = ["encrypted-ixs"]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
