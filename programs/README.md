# /programs — Anchor workspace (Builder B)

Anchor 1.0.2 + Solana CLI 3.1.x + bun. Two programs:

- `programs/risk_policy/` — encrypted policy storage + Squads-gated mutations + Arcium MPC integration. Spec: `.planning/builder-b/PRD.md` §2.1.
- `programs/swig_delegation/` — bounded execution wrapper + slippage gate + write-side idempotency. Spec: `.planning/builder-b/PRD.md` §2.2.

## Build / test

```bash
anchor build           # build both programs (release SBF + native test profile)
anchor test            # spin up local validator + ts-mocha tests
```

## Notes

- Default Anchor stub program (named `programs`) was deleted; only the two real programs above are workspace members.
- Devnet program IDs land in `Anchor.toml` `[programs.localnet]` after `anchor keys sync` during deploy. Current values in this file are the dev keypair pubkeys generated by `anchor new`.
- `target/`, `.anchor/`, `test-ledger/` are gitignored at the repo root.
